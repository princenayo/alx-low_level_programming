#include "main.h"
/**
 * _abs -a function that computes the absolute value of an integer
 * @n: int to be checked
 * Return: absolute value of the int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
